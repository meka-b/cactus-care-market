## 2025-02-18 - Hardcoded Admin Credentials \n**Learning:** Replaced hardcoded credentials in database seeding with environment variables using `os.environ.get`, ensuring backward compatibility with fallback values. Also updated integration tests similarly so they won't break if CI sets an environment variable.\n**Action:** Whenever generating default values for administrative or demo accounts, always use environment variables with fallback values.
